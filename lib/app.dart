import 'package:flutter/material.dart';

class AkgecErpApp extends StatelessWidget {
  const AkgecErpApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: const AkgecErpApp(),
    );
  }
}
